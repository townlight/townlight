# Changelog

All notable changes to the civicsuite umbrella repo are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## Release recovery notice (2026-05-09)

The block of entries below dated 2026-05-01 through 2026-05-08 referring to "compatibility publication," "shipping records product," "shipping flagship," "v1.0.0," and similar promotion-language are **doc-update entries against tags that are now frozen pending recovery gates** (see `docs/release-recovery-status.md`). They are preserved here as historical record but should not be read as ship signals.

A release-workflow failure handoff (2026-05-07, retained project-side) explains the context: a lateral v1.0 sweep across multiple repos was halted by the project owner. Three of those repos (civicinspect, civicgrants, civicprocure) subsequently received v1.0.0 tags between 2026-05-07 and 2026-05-08 in a continuation of the same workflow violation. None of those tags constitute promotion.

This changelog will be split going forward: (a) doc / governance / spec changes go here; (b) per-module ship signals go in each module's own CHANGELOG. Inline version numbers in this file may be stale; the canonical pairing source is [docs/compatibility/index.md](docs/compatibility/index.md).

---

## [Unreleased]

### Changed

- **2026-09-08.** fix(model-pin): re-pin the Gemma 4 12B QAT Q4_0 GGUF to the current Hugging Face blob (size `6975879296`, sha256 `93567e57a8fe10b23569b9d9ec38cd005deedf71e29477c421a4b83f418a538b`) and change `resolve_url` from the mutable `resolve/main` pointer to the immutable revision `29d097773436b69ff9feafd636ab4cf873786537`. Google republished the blob on 2026-07-17, so the manifest sha256 (`faff1a63...`) no longer matched the file the app would download; with `checksum_required=true`, first-run model setup on the Records beta would have failed verification. The weekly `model-pin-check` now also requires a 40-hex HF revision in the URL and compares the live `X-Linked-Etag` (LFS sha256) to the manifest, so a silent upstream republish fails the check instead of only a size drift. Supersedes PR #247. Historical audit/evidence records keep the old hash as a factual record.
- **2026-09-08.** fix(org-refs): repoint live `github.com/CivicSuite/*` references to their `townlight/*` homes after the 2026-08-12 org transfer: the installer cleanroom checkouts, the Windows runtime PostgreSQL asset URL, the demo compose wheel URLs, the module scaffold template, and the living docs' repo links. Dated audits, evidence, agent-run records, and the release-provenance identities that were signed under the CivicSuite name are left as factual records. Supersedes PR #246; PR #245 (a prose rename of CivicSuite to Townlight across 102 docs) is closed because the living docs were restructured on 2026-08-20 through 2026-08-27 with an explicit historical-predecessor section that a blanket rename would corrupt.
- **2026-08-27.** release(records): complete the unsigned Townlight Records `1.1.0-beta.1` release-candidate gate on PR #248 candidate code head `dc777366a4ed810ec8899d77654edd6f2ab369e5`. Hosted CI passed the Windows MSI build, embedded real-runtime integration, install, launch, first run, backup/restore, repair, and uninstall lifecycle; the artifact remained explicitly non-publishable with `SignatureState=UNSIGNED` and `PublicationAllowed=false`. Local verification passed 197 Rust tests, 30 browser tests, desktop static/state tests, production build, dependency audit, suite-state checks, formatting, and a disposable Windows Sandbox lifecycle. Publication remains fail-closed until the merged `main` commit is signed as `CN=Scott Converse`, independently verified, rerun through the signed clean-machine journey, tagged at the exact signed SHA, and approved for publication.
- **2026-08-20.** feat(records): define the first Townlight Records Windows public-beta candidate as desktop version `1.1.0-beta.1`. Public product surfaces now use the Townlight and Townlight Records names while stable installer, application-data, package, import, environment, and database identifiers remain unchanged for upgrade compatibility. The default `records-beta` product profile installs and health-checks exactly Townlight Core, Records, Notice, and Access; Meetings and Code remain available only through the explicit legacy `city-core` compatibility profile and are not included in the Records beta runtime payload. The current desktop execution path remains the native Rust Records workflow, with the packaged Python modules identified honestly as embedded contract/reference services pending the named post-Records convergence gate.
- **2026-08-20.** fix(desktop): add the minimal version boundary for the temporary `city-work.json` store before Records beta. New state is written as schema version 1; an existing unversioned file is upgraded only after a verified local backup, using the existing atomic writer; and explicit unsupported versions fail closed without rewriting user data. This is bounded migration scaffolding for the current Rust execution path, not a commitment to keep JSON after the post-Records Python/Postgres convergence gate.
- **2026-08-19.** feat(records): add the explicit Townlight Records fictional-demo loader. A signed-in local admin can load Core's pinned Redstone Valley v1 fixture only into an empty local profile; the desktop validates the canonical fixture and artifact hashes, refuses real-city or unmarked replacement data through the pinned contract, creates a verified backup before mutation, writes city-work state atomically, verifies it by reading it back, and restores the prior state if verification fails. The Staff and Resident/Public Records surfaces carry the fictional municipality name, fixture version/SHA-256, and non-color-only synthetic-data watermark. Loading never occurs automatically. The imported request, search ground truth, public status, and tamper-evident audit entry remain local and preserve human approval before any response, release, fulfillment, or closure. This is beta test infrastructure, not a public-release or production-readiness claim.
- **2026-08-19.** ci(release): split routine Windows MSI validation from publication signing. Pull requests and ordinary `main` builds now produce a visibly named private `-UNSIGNED.msi`, exercise it through the existing integration and install/backup/restore/uninstall lifecycle, and record `PublicationAllowed=false` without requiring Azure credentials. A separate manual `main` dispatch with `sign_for_publication=true` remains fail-closed on Azure signing, signer `CN=Scott Converse`, timestamp, and two independent signature checks. Release automation now accepts only the `SIGNED` artifact from a successful manual run at the exact release-tag SHA and re-verifies the hash, evidence, Authenticode signer, timestamp, and `signtool` result before upload. Evidence parsing normalizes Windows CRLF before validating its key/value lines. The workflow comments now accurately describe the proven client-secret path without falsely claiming that OIDC federation depends on an Azure subscription. This unblocks development CI without weakening the rule that no unsigned installer may be published.
- **2026-07-13.** release: cut CivicSuite Windows Local **1.0.4** — a first-run wizard UX overhaul for non-technical city clerks, triggered by a real tester getting stuck creating the backup folder (the app's "local administrator" gate read as a Windows permission error). Bundles the 2026-07-12 non-technical-operator audit's fixes across three reviewed PRs: **Wave 1 (#241)** — "local administrator" → "CivicSuite admin" rename + the create-admin→backup stuck-point cluster; **Wave 2 (#242)** — the remaining Criticals (model-download working-state, sign-in lockout, module/health error mapping); **Wave 3 (#243)** — the Majors/Minors wording + styling sweep. Same six-module city-core suite as v1.0.3; Authenticode code-signed via Azure Trusted Signing. Desktop build version bumped `1.0.3` → `1.0.4` across `tauri.conf.json`, `desktop/package.json`, `desktop/src-tauri/Cargo.toml`, and the two lockfiles. Supersedes v1.0.3. See the three Wave entries below for the per-fix detail.
- **2026-07-12.** desktop: first-run wizard UX fixes (Wave 3 of the 2026-07-12 non-technical-operator audit — the Majors/Minors wording + styling sweep). Plain-English/jargon: "Set Up Services and Model" health button → **"Run Health Check"** (M2); the sign-in panel shows a readable role label instead of the raw `city-staff` slug (M10); "runtime services/settings" → **"background services"** (m1); the module step's build-pipeline jargon ("part of the architecture… package and proof contracts… clerk path") → plain language (M9); the model step no longer leads with "Gemma 4 12B quantization-aware weights" (m3); "Load in Ollama" button → **"Start Local AI Engine"** (m6); the Windows-login "profile" and module-bundle "profile" senses reworded so "profile" doesn't mean three different things across steps 1–3 (M8); the folder-cancel message no longer tells a solo clerk to ask "IT" (m5). Visual/affordance: a locked module row is now visually distinct from an active teal selection (M3); a genuine action **failure** renders red, distinct from the neutral amber "next step" hint (M4); the first-admin passcode field states the 10-character minimum up front (M5) with an inline hint on rejection (m4); a "Fix an earlier step anytime from Settings" line gives a recovery path out of the one-way wizard (M7); the five model-setup buttons get a "do these in order: 1 Download, 2 Verify, 3 Start" hint (m7); and a successful sign-in from the buried access panel now scrolls back to the step that was blocked (m8). No `data-*` values or accessible button names changed. Tests updated in lockstep; static-smoke + xss-and-state (T4–T15) and Playwright 29/29 green. Implemented via a cost-scoped sonnet worker with the same independent-review gate as Waves 1–2.
- **2026-07-12.** desktop: first-run wizard UX fixes (Wave 2 of the 2026-07-12 non-technical-operator audit — the remaining Criticals). (C1) The local AI model step now shows a **"Downloading…" working state and locks all model buttons the instant you click**, with copy that says CivicSuite may look frozen during the (synchronous, up-to-an-hour) download and to not close it — so a clerk can't re-click into a second multi-GB download or read the freeze as a crash. (C5) The **sign-in lockout message no longer contradicts itself**: when the backend reports "Too many failed sign-in attempts. Wait N seconds," the next-step now says to wait rather than "check the passcode and try again." (C6) A **blocked/not-ready module shows a plain-English reason** ("not available in this release yet; check back after your next CivicSuite update") instead of the raw build-contract string (e.g. "must target CivicCore 1.2.0"); the raw string is preserved only as a hover tooltip for support. (C7) The **Health step's model-not-verified block now names the exact control** ("Open the Local AI model step, click Verify Checksum…") instead of a vague "finish Local AI model setup." Behavioral tests added (xss-and-state T14 model working-label, T15 module plain-reason — both proven to fail without their fix) plus static-smoke tripwires for all four; cargo 181/181, Playwright 29/29, npm test green. Live model-download *progress-bar animation* (backend progress events) is a deeper follow-up tracked in the audit report — this wave removes the "is it frozen / double-click" hazard, not the missing progress bar.
- **2026-07-12.** desktop: first-run wizard UX fixes (Wave 1 of the 2026-07-12 non-technical-operator audit; a real tester got stuck creating the backup folder). (1) **Terminology:** renamed the app account from "local administrator" → **"CivicSuite admin"** across every user-facing setup/sign-in/model/gate string (JS + Rust + `windows-first-run.json`), removing the collision with the Windows "local Administrators group" that made a Windows admin read the block as an OS-permission error. The internal `local-admin` role slug is unchanged. (2) **C2:** the "Choose Folder" button (and its path input) now lock together with the step's primary action when the CivicSuite-admin gate is active, instead of one button being greyed while the one next to it silently invites the exact failure the tester hit. (3) **C3:** the step's own "Action needed" box now stays on the card after a failed action and shows that failure's next step in place, instead of vanishing and relocating the only explanation to a banner below all eight wizard cards. The failure text is scoped to the step that produced it (`actionResult.forStepId`), so a failure from another context (e.g. a Settings module action) can no longer surface on the wrong wizard step. The living operator docs (README, USER-MANUAL, FAQ, STATUS) were renamed to "CivicSuite admin" in the same change so they match the in-app wording. (4) **C4:** first-run save errors now map raw `Missing required setup field: <key>` messages to the on-screen field label and an actionable "Enter a value for {label}, then save," instead of a fixed "Correct the setup information" with a raw camelCase key. (5) **M1:** the folder-picker error only tells you to sign in when the error is actually the admin gate; other OS/picker errors get a neutral remedy. (6) **m2:** the passcode field uses the same label ("CivicSuite passcode") at account creation and at sign-in so a returning admin can find it. Tests updated in lockstep and green (static-smoke incl. new Wave-1 guards, xss-and-state, cargo `fmt`+`test` 181/181, Playwright 29/29). Later waves (model-download feedback, lockout-message contradiction, module/health error mapping, jargon/styling sweep) tracked in `Desktop/CODE/civicsuite-firstrun-ux-audit-2026-07-12-v1.0.md`.
- **2026-07-11.** release: cut CivicSuite Windows Local **1.0.3** — the **first Authenticode code-signed** Windows-Local release. Signing is performed in GitHub Actions via **Azure Trusted Signing** (HSM-held Microsoft-rooted certificate, fail-closed `signtool verify /pa` in-workflow; proven end-to-end on run `29141208093`). This release bundles the same six-module city-core suite as v1.0.2 with the unsigned-installer / Windows-SmartScreen warning surfaces removed from the first-run wizard and living docs (see the two entries below). Desktop build version bumped `1.0.2` → `1.0.3` across `tauri.conf.json`, `desktop/package.json`, `desktop/src-tauri/Cargo.toml`, and the two lockfiles. Supersedes v1.0.2.
- **2026-07-11.** desktop: remove the unsigned-beta and SmartScreen warning steps from the CivicSuite desktop first-run wizard now that the Windows MSI is Authenticode code-signed via Azure Trusted Signing (proven: CI run 29141208093, signtool-verified). Dropped the `unsigned-beta`/`smartscreen` steps and the dead `review` action from `desktop/runtime/windows-first-run.json`, `desktop/src-tauri/src/first_run.rs`, and the `desktop/src/main.js` fallback wizard state; the wizard now starts at "Install and local data locations". Removed the `licenseFile` MSI surface from `tauri.conf.json` and deleted `desktop/installer/windows/unsigned-beta-install-notice.txt`. Removed the corresponding `UnsignedBetaNotice`/`UnsignedBetaNoticeSurface`/`SmartScreenGuidance` evidence lines from the `desktop-windows-msi.yml` build workflow. Rust and static-smoke test suites updated in lockstep and green. (The separate Docker/Inno-Setup "starter set" installer contract in `scripts/plan-installer.py` / `scripts/verify-installer-plan.py` / `docs/installer/starter-set-release-contract.md` was left untouched — that installer line has not been Authenticode-signed and its unsigned-beta/SmartScreen guidance still reflects reality.)
- **2026-07-11.** docs: remove unsigned-beta and SmartScreen warning surfaces. Windows MSI artifacts are Authenticode code-signed via Azure Trusted Signing; removed "Unknown Publisher" / "More info" / "Run anyway" walkthrough narrative and "unsigned beta" framing from README.md, USER-MANUAL.md, FAQ.md, SUPPORT.md, STATUS.md, ANNOUNCEMENT.md, and RELEASING.md. Checksum-verification guidance retained where present. Living-document surfaces updated only; historical/point-in-time records (audits, ADRs, release-recovery-status, etc.) preserved unchanged.
- **2026-07-11.** docs: remove unsigned-beta and SmartScreen warning narrative from CODE_SIGNING_POLICY.md. Windows MSI artifacts are now code-signed via Azure Trusted Signing with HSM-held Authenticode certificate; all signing occurs in GitHub Actions CI with in-workflow verification. Updated policy to document current reality: what is signed, how (Azure + signtool /pa), author/approver, and distribution source.
- **2026-06-13.** chore(installer): advance the CivicCore city-core `source_commit` pin to merged main head `1a53f0680fffce34efeb939cbeb9915b6e208d6c`. The package version remains `v1.2.0`; this source pin adds the Windows-local platform contracts and PostgreSQL-backed task queue/worker needed by the Tauri/WebView2 desktop runtime supervisor.
- **2026-06-13.** chore(ledger): record CivicClerk v1.0.4 as the current city-core meeting workflow release car while preserving the suite boundary. CivicClerk v1.0.4 was published from tag commit `9f63ab79a0a75611ec3221d77e8577a95501e4c7`; the release workflow preflight and verify-build jobs passed in run `27451630904`, and PR #176 repaired the draft-release asset upload workflow for future tags. The suite installer vendored-source pin advances to CivicClerk `main` head `dae807ec9d1370dd22cf6aba88e4c6fc6b4168d5`, which includes the workflow-only release repair. The same lockstep pass also refreshes CivicRecords AI's source pin to post-PR-#102 `master` head `538766523ad90ee7553b0ffa75b626d3d4850b17` without changing the published v1.7.3 release object. This does not create a new city-ready, procurement-ready, production-ready, macOS lifecycle, public-use, or full-suite promotion claim.
- **2026-05-28.** fix(audit): continue the all-findings audit-team closure pass for run `2026-05-28-city-core-real-non-technical-release`. The closure batch now records shared suite-session revocation persistence, bounded fallback revocation caches, Records AI prototype-token shell work, launcher discoverability/config cleanup, audit-gate authority policy, audit artifact completeness policy, workflow-cost replay policy, user-manual artifact currency policy, naming-honesty policy, and refreshed source-pin lockstep evidence. Final status promotion remains blocked until independent `audit-team-claude` reruns against the final five heads.
- **2026-05-30.** chore(recovery): restore the post-deletion Stage 0 recovery baseline and installable local pre-push hook source so city-core work is committed and pushed slice-by-slice instead of accumulating as a dirty worktree.
- **2026-05-30.** chore(installer): restore the recovered CivicCode city-core `source_commit` pin to post-PR-#76 `main` head `a960bba0a2249d118b593dd61bee3a65a69a9d77`, preserving the already-published module release objects while matching the live module default-branch state.
- **2026-05-29.** test(auth): add a city-core suite-session contract test that issues one CivicCore token, validates it through Records AI, CivicClerk, and CivicCode adapters, then revokes it once through the shared revocation file. The CivicCode fixture now uses the searchable `CIVICCORE_SUITE_SESSION_SECRET` literal, and the CivicCode source pin advances to the post-PR-#75 `main` head `9284fd1a0704541b3422e5dd0ba47bea3713825a`.
- **2026-05-29.** chore(installer): update city-core `source_commit` pins for the merged real non-technical-release module heads. CivicCore now pins to `9f7e3a5a0156fca779b48076d49c13181d15151c`, CivicRecords AI to `ae34a499c1e0794d3322146369f798f19bd0a146`, CivicClerk to `f39d0eeccc6804b86c542b4cdffe4fab0665d503`, and CivicCode to `9284fd1a0704541b3422e5dd0ba47bea3713825a` for local vendored-source verification without changing the already-published release objects. The installer-cleanroom workflow checks out those same source roots so CI packaging and `installer/modules.json` stay in lockstep.
- **2026-05-27.** docs: clean city-core operator truth surfaces. FAQ now frames forbidden public-use/procurement/production/full-suite/macOS claims only as negated boundaries and keeps CivicAccess out of the city-core path. README, STATUS, USER-MANUAL, docs index, troubleshooting, unified spec, recovery status, and downstream pins now point operators at the suite launcher, Linux Docker signed-repository bootstrap, local shared launcher session boundary, and live regenerated hash/source-pin/attestation trust path instead of restored `installer/dist` artifacts.
- **2026-05-27.** chore(installer): add explicit `source_commit` fields to `installer/modules.json` and enforce them in `scripts/plan-installer.py`, `scripts/run-clerk-core-installer.py`, `scripts/verify-suite-state.py`, and the installer-cleanroom workflow source checkouts. This keeps the vendored-source installer path reproducible without switching to published wheels. CivicRecords AI now pins to `59cabcbe5072d0c843fd57356a7d113bf90537f1`, the post-PR-#100 merge commit carrying the CRIT-1 mobile skip-link fix.
- **2026-05-27.** docs: reconcile city-core beta-ready truth after audit-full. PR #183 has green verify, release-lockstep-gate, and installer-cleanroom checks on head `5654f5e`; local Windows and Linux first-run browser QA evidence is preserved (internal run evidence, retained project-side), and audit-full records zero unresolved Blocker or Critical findings. The honest state is beta-ready truth-reconciled; this still does not claim public-use readiness, city-ready status, procurement readiness, production readiness, macOS lifecycle certification, or full-suite release.
- **2026-05-27.** ci: close city-core PR evidence loop. PR #183 has green verify, release-lockstep-gate, and installer-cleanroom checks, including Linux lifecycle for clerk-core and city-core archives plus Windows readiness and macOS beta readiness; exact volatile PR run IDs are recorded in the PR body and run evidence. CivicRecords AI PR #100 is green at `d7f84a3` for CI run `26487863170`. At this checkpoint the remaining promotion caveat is audit-full; this still does not claim public-use readiness, city-ready status, procurement readiness, production readiness, macOS lifecycle certification, or full-suite release.
- **2026-05-27.** fix: close city-core wrapper first-run browser QA. Windows `.cmd` and Linux `.run` one-click artifacts now have local first-run browser evidence through wizard, lifecycle install, forced Records AI admin password rotation, old generated credential rejection, rotated credential login, Records AI dashboard, CivicClerk staff surface, and CivicCode search at desktop and mobile widths. Windows WSL lifecycle forwarding now preserves `CIVICSUITE_FIRST_ADMIN_EMAIL`, `CIVICSUITE_INSTALLER_PORT_OFFSET`, `CIVICSUITE_INSTALLER_PROJECT_SUFFIX`, and `DOCKER_CONFIG`; the lifecycle runner retries transient Docker Compose dependency-health races. Remaining promotion caveat is audit-full; this still does not claim public-use readiness, city-ready status, procurement readiness, production readiness, macOS lifecycle certification, or full-suite release.
- **2026-05-26.** chore: record city-core non-technical installability truth. The city-core profile now has regenerated vendored-source Windows and Linux one-click artifacts at installer version `0.1.2`, Guided/Manual Docker prerequisite setup paths, first-run wizard smoke evidence, 60 GB matching-host cleanroom hygiene, and local Windows/Linux install-repair-verify-backup-restore-uninstall lifecycle evidence. The initial honest status required CI and audit evidence; the 2026-05-27 entry records CI closure. This does not claim public-use readiness, city-ready status, procurement readiness, production readiness, macOS lifecycle certification, or full-suite release.
- **2026-05-25.** v0.1.2 - city-core profile defaults records-ai to PORTAL_MODE=public. A records system without public submission is not a city records system. Smaller profiles retain the private default unless explicitly flipped.
- **2026-05-25.** chore: align city-core installer truth with CivicRecords AI v1.7.3. The release-asset convention bring-up did not change installer behavior, but the rebuilt vendored-source city-core installer now expects the v1.7.3 Records health version and records the current module-car version consistently.
- **2026-05-23.** docs: add module-extensibility checklist and scaffold template. New module work now has a suite-level checklist, copyable baseline files, and a warning-only scaffold layout check in `verify-suite-state.py`.
- **2026-05-23.** chore: city-core release-train truth reconciliation. CivicCore v1.2.0, CivicRecords AI v1.7.2, CivicClerk v1.0.3, and CivicCode v1.0.8 are the city-core release cars; CivicRecords AI v1.7.2 supersedes v1.7.1 after the clean installer lifecycle exposed and repaired a bad frontend lockfile tarball reference. CivicAccess is excluded after a NEEDS-WORK depth probe; CivicZone, CivicPlan, CivicPermit, and CivicInspect are recorded as no-functional-upgrade v0.2.2 demotion releases queued for Tier 2 real work. This prepares the umbrella truth for the city-core installer and integration-proof caboose without claiming city-core is released beyond beta-ready status.
- **2026-05-21.** chore: CivicPermit v1.0.0 suite-truth reconciliation. Superseded by the 2026-05-23 v0.2.2 no-functional-upgrade demotion truth; queued for Tier 2 real work.
- **2026-05-21.** chore: CivicInspect v1.0.0 suite-truth reconciliation. Superseded by the 2026-05-23 v0.2.2 no-functional-upgrade demotion truth; queued for Tier 2 real work.
- **2026-05-21.** chore: CivicPlan v1.0.0 suite-truth reconciliation. Superseded by the 2026-05-23 v0.2.2 no-functional-upgrade demotion truth; queued for Tier 2 real work.
- **2026-05-21.** chore: CivicZone v1.0.0 suite-truth reconciliation. Superseded by the 2026-05-23 v0.2.2 no-functional-upgrade demotion truth; queued for Tier 2 real work.
- **2026-05-21.** chore: CivicCode v1.0.0 suite-truth reconciliation. Superseded by CivicCode v1.0.8 in the 2026-05-23 city-core release train.
- **2026-05-21.** chore: CivicAccess v1.0.0 suite-truth reconciliation. Superseded by the 2026-05-23 NEEDS-WORK depth probe; CivicAccess is OUT of city-core until gap closure and re-probe.
- **2026-05-21.** release: promoted the bounded Clerk-Core public-use starter release. The final release truth points at `installer-clerk-core-v0.1.0`, main verify run `26210542980`, main installer-cleanroom run `26210542979`, Windows matching-host lifecycle evidence, macOS beta-level archive/readiness evidence, and the Clerk-Core release-gate audit with no unresolved Blocker or Critical findings. Scope remains CivicCore + CivicRecords AI + CivicClerk only; this is not a full-suite, procurement, production hosting, airgap, live cross-module records exchange, or macOS lifecycle certification claim.
- **2026-05-20.** docs: added Clerk-Core installed route/state matrix evidence. The new evidence records CivicRecords AI and CivicClerk public/staff/API route inventory, desktop/mobile browser QA, UI state checks, adversarial local integration mocks, and missing-backup restore-precondition behavior while keeping the public-use gate RED pending independent audit and final promotion evidence.
- **2026-05-21.** docs: recorded regenerated Clerk-Core final package evidence after CivicClerk main `45eaccfcc69dd1ae7e2e45d7badd5d188b49397d`. The evidence records new unsigned Windows/macOS/Linux archive checksums, Windows matching-host install/repair/verify/workflow/backup/restore/uninstall proof, macOS beta-level archive/readiness proof, and a hardened structured public-use matrix verifier while keeping the public-use gate RED.
- **2026-05-20.** docs: added the Clerk-Core public-use readiness gate. The new gate maps beta.4 evidence to the release-recovery checklist, records RED/YELLOW blockers before any promotion beyond outside-test beta, refreshes the docs landing page, and adds a docs verifier check for current-facing starter-product overclaims.
- **2026-05-20.** release: published clerk-core beta.4 installer artifacts. GitHub prerelease `installer-clerk-core-v0.1.0-beta.4` points at synced main SHA `4aee5355e4a9bdb56850a16d3a10693e706f9278` and supersedes beta.3 without rewriting the public beta.3 tag. This remains an unsigned OSS beta outside-test release only; it is not a public-use, city-ready, procurement-ready, production-ready, full-suite, live cross-module exchange, or macOS lifecycle certification claim.
- **2026-05-19.** release: published clerk-core beta.3 installer artifacts. GitHub prerelease `installer-clerk-core-v0.1.0-beta.3` now points at main SHA `a3ca9d75dc51f7e0928671b30c1693eca3a3fcae` and includes Windows, macOS, and Linux clerk-core installer archives, `SHA256SUMS`, and the release manifest. This is an unsigned OSS beta outside-test release only; it is not a public-use, city-ready, procurement-ready, production-ready, full-suite, live cross-module exchange, or macOS lifecycle certification claim.
- **2026-05-19.** docs: clerk-core beta.3 release gate package. The release-gate package prepared `installer-clerk-core-v0.1.0-beta.3` after PR #150/#153/#155 evidence, requiring a `release-tag` PR, release-lockstep, and main CI before publication; this was an unsigned OSS beta decision, not a public-use, city-ready, procurement-ready, live cross-module exchange, or macOS lifecycle certification claim.
- **2026-05-19.** fix: clerk-core package cleanroom evidence baseline. PR #153 preserves Windows readiness/plan evidence when extraction cleanup hits a Windows file lock, and PR #155 syncs the public docs to main verify run `26116871355` plus installer-cleanroom run `26116871385`.
- **2026-05-19.** fix: clerk-core CivicRecords port isolation. PR #150 writes resolved CivicRecords AI API/web ports into the copied runtime `.env`, keeping installed package lifecycle runs on isolated CI ports.
- **2026-05-18.** chore: clerk-core installed workflow proof. The extracted-package cleanroom lifecycle can now request optional installed-stack workflow proof for CivicRecords AI request/search-surface/review/response handling and CivicClerk agenda/packet/minutes/vote/notice/archive handling. This remains lifecycle and workflow evidence for the starter profile only; it does not claim public-use readiness, live cross-module records exchange, or macOS lifecycle certification.
- **2026-05-18.** chore: clerk-core city release target setup. The active control-plane target is now the CivicCore + CivicRecords AI + CivicClerk starter product, the v0.9.0 agent-pipeline scope-lock plumbing is present, CivicRegWatch and CivicAPI are recorded as planned non-selectable installer modules, and no public-use/product-ready claim is made.
- **2026-05-18.** fix: civicrecords-ai v1.6.1 suite-truth verifier alignment. The suite-state verifier and unified spec now treat CivicRecords AI [v1.6.1](https://github.com/CivicSuite/civicrecords-ai/releases/tag/v1.6.1) as the current developer-preview records release, while preserving v1.6.0 as the historical B2 Docker secret-file recovery.
- **2026-05-15.** chore: installer cleanroom certification truth. Package cleanroom reports now carry explicit evidence classification, host/platform matching, mutability, and certification-scope fields; the clerk-core lifecycle runner resolves run-id/CLI-controlled ports and Compose project names; the installer workflow drops the daily cron and keeps concurrency plus short artifact retention. Linux remains the matching-host lifecycle proof path; Windows and macOS wrapper evidence remains bounded until matching-host lifecycle evidence is recorded on those hosts.
- **2026-05-14.** chore: installer truth-hygiene ledger. Recorded the prior installer truth-hygiene work in the umbrella changelog without promoting the suite: the clerk-core package remains an unsigned OSS beta, macOS remains archive/readiness only, and native installer/signing plus procurement-readiness claims remain future gates.
- **2026-05-12.** chore: civicrecords-ai v1.6.0 B2 audit punch-list closure. CivicRecords AI [v1.6.0](https://github.com/CivicSuite/civicrecords-ai/releases/tag/v1.6.0) closes audit punch-list B2 by moving `JWT_SECRET` and `FIRST_ADMIN_PASSWORD` material into Docker Compose secret files and removing the `_FILE` pointer env names from the container env. The literal `docker compose exec -T api env | grep -E "JWT_SECRET|FIRST_ADMIN_PASSWORD"` acceptance command now returns zero lines. CivicCore pin remains at v1.0.1.
- **2026-05-10.** chore: civiccore v1.0.1 suite-truth reconciliation. The umbrella truth surface now records CivicCore [v1.0.1](https://github.com/CivicSuite/civiccore/releases/tag/v1.0.1) as the current shared-platform recovery patch, including auth-error-payload hardening documented by the civiccore repo's [`docs/audits/civiccore-audit-full-2026-05-07.md`](https://github.com/CivicSuite/civiccore/blob/main/docs/audits/civiccore-audit-full-2026-05-07.md). Downstream CivicCore pins for CivicClerk, CivicCode, CivicZone, CivicPlan, CivicPermit, CivicInspect, CivicGrants, and CivicProcure now reconcile to the v1.0.1 wheel.
- **2026-05-10.** chore: civicclerk v1.0.1 suite-truth reconciliation. CivicClerk [v1.0.1](https://github.com/CivicSuite/civicclerk/releases/tag/v1.0.1) is now the current recovery label after the QA-001 security-default fix. Fresh installs deny anonymous staff writes by default, `open` mode is explicit opt-in for local rehearsal, and installer truth now records the protected default.
- **2026-05-11.** chore: civicrecords-ai v1.5.0 / civiccore v1.0.1 alignment. CivicRecords AI [v1.5.0](https://github.com/CivicSuite/civicrecords-ai/releases/tag/v1.5.0) now consumes CivicCore v1.0.1, restoring a unified active-suite CivicCore pin and re-enabling the full-suite installer profile.
- **2026-05-11.** chore: civiccore v1.1.0 staff-key gate suite-truth reconciliation. CivicCore [v1.1.0](https://github.com/CivicSuite/civiccore/releases/tag/v1.1.0) adds `staff_key_gate`, a shared timing-safe staff-key helper that closes audit D2/B3 for the six downstream modules in this rollout: CivicCode, CivicPlan, CivicPermit, CivicInspect, CivicGrants, and CivicProcure.
- **2026-05-09.** Reframed the 2026-05-01 → 2026-05-08 changelog cluster as doc-update entries (release recovery notice above).

### Added

- **2026-05-09.** Drafted umbrella documentation rewrite suite at `audit-civicsuite-2026-05-09/doc-rewrites/`: refreshed README, new STATUS.md (module-by-module honest status), new FAQ.md (civic-operator FAQ), new ARCHITECTURE.md (with Mermaid suite diagram), refreshed USER-MANUAL.md with "Your first task" walkthrough, and updated release-recovery-status.md with drift incident log for the three v1.0.0 drift repos.
- **2026-05-09.** Documented the three v1.0.0 drift repos (civicinspect, civicgrants, civicprocure) in the recovery-status doc.

---

## [civicsuite-windows-local-v1.0.2] - 2026-07-02

### Changed

- **2026-07-02.** release: publish CivicSuite Windows Local **1.0.2** as **Latest** (GA candidate / open public beta). The [`civicsuite-windows-local-v1.0.2`](https://github.com/CivicSuite/civicsuite/releases/tag/civicsuite-windows-local-v1.0.2) release (MSI SHA-256 `bbdeb1b69e846d3ccb8c961502f4b2f158e92623e7bf4dfa9d4c4bf2f9a0fd02`, 1,646,999,452 bytes, built by public run `28626482190` on main merge `0b797c4`) ships the six-module city-core suite — the first release bundling **CivicAccess v0.4.0** with its on-screen Accessibility tab and three local-AI drafting features — plus the suite-wide `/api/chat` generation-quality fix and the clean-machine VC++ runtime fix (both detailed in the entries below). **Phase D clean-VM acceptance: PASS** across two Windows Sandbox runs; the evidence kit is committed under `docs/evidence/civicaccess-v102-phaseD-2026-07-02/`. The release docs sweep merged post-tag (PR #222: released-tense README/STATUS/ANNOUNCEMENT with two-voice sections, compatibility matrix v1.0.2 Latest, PROVENANCE CivicAccess pin, canonical-design landing page); seed Discussions #224–#227 posted; the org front door was established (`CivicSuite/.github` profile README pointing at the landing page; stray GitHub Pages on civiccore and civicrecords-ai retired). Same beta posture as v1.0.1 — Authenticode code-signing (SignPath, in progress) remains the single gate to GA.

- **2026-07-02 (phase-d-vcruntime).** fix(citycore): **the bundled PostgreSQL could not start on a clean Windows machine — the release-grade Phase D caught it, root-caused, fixed.** Running the shipped v1.0.2 MSI in a clean Windows Sandbox (the canonical fresh clerk PC), the first-run health gate could not clear: `initdb.exe` failed with a native `VCRUNTIME140.dll was not found` system error, so `postgres` (15432) and everything that depends on it — `python-services` (15480), `task-queue`, `task-queue-schema` — stayed down. **Root cause:** the portable EnterpriseDB PostgreSQL 17 binaries link against the Microsoft Visual C++ runtime, but the portable-ZIP extraction (`Expand-PostgresServerPayload`) ships only `bin/include/lib/share` — it deliberately skips the VC++ redistributable that the EDB *installer* normally lays down. A clean Windows has no system-wide VC++ redist, so the DLLs are absent. **The smoking gun:** the same runtime *is* bundled for **Ollama** (`payload/ollama/lib/ollama/cuda_v*/vcruntime140.dll`) but not for PostgreSQL — which is exactly why the local AI model loaded and served fine while the data store failed. This is the same class of defect as the earlier `zlib1.dll` runtime-dependency fix (2026-06-18), pre-existing across v1.0.x (not a v1.0.2/CivicAccess regression), and structurally invisible to CI because GitHub's `windows-latest` runners have the VC++ redist installed system-wide (the MSI-lifecycle + integration jobs run green there). **Fix:** `desktop/scripts/prepare-runtime-payload.ps1` now stages the x64 VC++ runtime DLLs (`vcruntime140.dll`, `vcruntime140_1.dll`, `msvcp140.dll`, and the rest of the `Microsoft.VC*.CRT` set) into `postgres/bin` next to the binaries — located via `vswhere` from the Visual Studio/Build Tools install already required for the pgvector `nmake` build, so it is reproducible in CI and license-clean (these are the redistributable CRT DLLs). Windows resolves an EXE's imports from its own directory first, so this is self-contained — no system-wide install needed. The three essential DLLs are added to the postgres payload's `required_files` in `windows-runtime-payloads.json`, so the payload-lock step (and the desktop supervisor's `payload_required_files_present` check) now **fail loudly** if the VC++ staging ever regresses, and their SHA-256 is recorded in the lock. Embedded CPython already ships its own `vcruntime140.dll`, so this is a postgres-only fix. Test fixture `write_test_postgres_payload` updated to stage the DLLs so the synthetic runtime matches the manifest. 181/181 cargo, static-smoke + xss PASS, `cargo fmt --check` clean. Re-verification: a release-grade Phase D run against the rebuilt MSI must confirm the full first-run health gate clears on the clean VM. Full evidence: `civicsuite-phaseD-1.0.2\evidence\` (Cowork-local).

- **2026-07-02 (generation-tuning).** perf(citycore): raise the shared local-generation output/context bounds now that the `/api/chat` fix produces real text worth completing. `model.rs` `LOCAL_GENERATION_NUM_PREDICT` **192 → 512** and `LOCAL_GENERATION_NUM_CTX` **3072 → 8192**; temperature stays **0.2**. **This supersedes the "num_predict stays 192" note in the (phase-d-fix) entry below.** Evidence: a live sampling A/B against the real pinned model loaded in the Phase D sandbox (same runtime the fix was proven on) showed 192 truncates the Accessibility Review remediation analysis mid-sentence, while 512 lets it finish (a ~403-token analysis completed in ~88 s, well inside the unchanged 180 s `LOCAL_GENERATION_TIMEOUT_MILLIS`); the same A/B showed temperature 0.2 ≈ 1.0 on these constrained government-text tasks but cleaner and more reproducible (better for public-records output), so Google's recommended 1.0/top_p/top_k was **not** adopted — the earlier garbage was 100% the raw/turn-format bug, not temperature. `num_ctx` 8192 covers a near-maximum civic-document input plus the larger draft with headroom; validated against the corrected **32 GB** RAM target (the 12B Q4 ≈ 7 GB of weights leaves ample room for the bigger KV cache). The **12B pin is retained for v1.0.2** (proven end to end through Phase D, ~7 GB first-install download, still runs on 16 GB machines); the 26B-A4B MoE variant that 32 GB makes viable is logged as a post-release quality A/B, not a release-blocking swap. Shared-helper change → applies to all four AI features (CivicClerk minutes, CivicRecords response, CivicCode guidance, the three CivicAccess features); the request-shape unit test's asserted `num_predict`/`num_ctx` values updated to match. No user-facing copy pins a token count, so none changed.

- **2026-07-02 (phase-d-fix).** fix(citycore): **the local-AI features produced garbage output against the real model — root-caused and fixed.** A live Phase D dress rehearsal (real pinned Gemma model downloaded + SHA-verified + loaded into the bundled Ollama, all six readiness checks green, driven through the real Tauri bridge in Windows Sandbox) caught what the GauntletGate rounds structurally could not: the `CIVICSUITE_FAKE_MODEL_RESPONSE` seam the gate ran against returns canned text, so it proved the pipeline (labels, dual-path fallback, human-review gating, audit framing — all correct) but by construction never tested real generation QUALITY. Against the real model the actual text was unusable — plain-language rewrite and translation echoed the input with a stray `<|channel>thought` control marker; the review analysis was digit-spam. **Root cause (isolated live, side-by-side on the same model+runtime):** `model.rs::generate_local_text` posted to `/api/generate` with `raw: true` and a hand-built `<start_of_turn>` Gemma turn-format prompt. The pinned model loads in Ollama 0.30.10 with `RENDERER gemma4` / `PARSER gemma4` (confirmed via `ollama show`); `raw: true` **bypasses** that renderer/parser, so the model's internal control tokens leak and generation degenerates. `/api/chat` on the identical model — "say hello" → clean `hello`; the three real civicaccess prompts → a genuine plain-language rewrite, a correct German translation, and a useful structured remediation analysis. **Fix:** switch `generate_local_text` from `/api/generate` + `raw:true` + manual turn markers + stop tokens to `/api/chat` with a single user message and `think:false`, letting Ollama apply the model's own template + gemma4 parser. This is a **shared-helper fix — it repairs all four AI features suite-wide** (CivicClerk minutes, CivicRecords response, CivicCode guidance, and the three CivicAccess features), which had all been on the broken `raw:true` path. The response parser already read `message.content`, so no parser change; the request-shape unit test now asserts the chat shape and forbids `raw`/`prompt`/`stop`; the static-smoke pin flips `/api/generate` → `/api/chat` so it can't silently revert. 181/181 cargo, static-smoke + xss PASS, `cargo fmt --check` clean. num_predict stays 192 (the documented R3 draft ceiling; output is a human-reviewed draft). End-to-end real-model re-verification of the code change (beyond the API-level proof) belongs to the release-grade Phase D run against a rebuilt MSI. Full evidence: `civicsuite-phaseD-1.0.2\evidence\RUN-NOTES.md` (Cowork-local).

- **2026-07-02 (later).** fix(citycore): close GauntletGate AI round-1's punch list on PR #220 (verdict was DO NOT ADVANCE at the zero-findings bar: 1 Major, 8 Minor, 9 Nit deduplicated across 7 independent reports) plus two CI-surfaced infrastructure bugs. **The Major (P1):** the AI features' mid-flight generation-failure arms (model ready at probe time, generation then fails — the diff's only novel failure semantics) were untested AND untestable, because the `CIVICSUITE_FAKE_MODEL_RESPONSE` seam can only succeed; added a mirrored `CIVICSUITE_FAKE_MODEL_ERROR` seam (cfg!(test): availability reports true, generation fails) + 3 tests proving the review degrades-and-saves while rewrite/variant fail fast with state and audit untouched. **The disclosure gap (P2):** the review's result box was silent when the AI analysis was skipped or failed (audit-chain-only disclosure, contradicting the design and the guided panel's own "and says so" promise) — the result now discloses the AI outcome in all three arms. **The rendering gaps (P3/P5):** multi-line model output rendered with newlines collapsed and long unbroken tokens (e.g. URLs the prompts require the model to preserve) stretched the page ~1400-1700px — `white-space: pre-line` + `overflow-wrap: anywhere` on the result box and AI-analysis block (+ wrap on review titles), locked by a permanent Playwright test that also proves the stored-analysis surface XSS-inert. **The honesty gaps (P4/P6/P10/P11):** rewrite/variant guided panels claimed "uses the verified local AI model" even when not ready (now ternary on readiness, both states asserted); banner condition unified through `aiEngineReady()`; rewrite button renamed into its confirm-label family. **The doc gaps (P7/P9):** the superseded no-AI plan-of-record survived un-annotated in 3 roadmap phase manifests (design risk R8 verbatim — now SUPERSEDED-annotated with dates); DESIGN.md corrected (result-note semantics, untracked-bundle premise, new num_ctx-truncation risk row R3b). **Test hygiene (P8/P13):** the gated real-Ollama test's `contains("pay")` assert was satisfiable by the input's own "payment" (now "must pay before"); both gated real-runtime tests get drop-guard service stoppers so a panicking assert can't orphan spawned processes. **CI-surfaced #1:** the `installer-cleanroom` city-core Linux archive lifecycle had been failing since the Phase C 5→6 flip — `run-clerk-core-installer.py` never learned `civicaccess` (argparse rejected it) — wired end to end per the civicnotice precedent (ports/env/compose/health/backup/restore) plus a real workflow proof (tokened `POST /review` → `GET` round-trip + tokenless-403 boundary; upstream facts verified at the pinned module commit); **confirmed green in CI on this PR**. **CI-surfaced #2:** running two gated payload tests in one process exposed that `with_temp_state_dir_and_payload`'s cleanup blindly deleted the CI-supplied `CIVICSUITE_RUNTIME_PAYLOAD_DIR` env var out from under the second test — the helper now restores each var's prior value. 181/181 cargo (3 new), 29/29 Playwright (1 new), static-smoke + xss PASS, fmt clean. Full findings: `gauntletgate-civicaccess-ai-2026-07-02-round1/` (Cowork-local).

- **2026-07-02.** feat(citycore): **CivicAccess now uses the suite's local AI engine** — closing the gap between the module's feature names and what actually ran behind them (project-owner directive; supersedes the roadmap's "no live AI" plan-of-record rule, amended in `docs/roadmap/civicaccess-citycore-integration/README.md`). (1) **Plain-Language Rewrite** drafts a real plain-language rewrite of the entered text via `model.rs::generate_local_text` (the same pinned local Gemma path CivicClerk/CivicRecords/CivicCode already use); the old 6-entry jargon-map pass is now the explicit deterministic fallback, labeled "AI engine not ready" when it runs. (2) **Multilingual Variant** drafts a translation of the actual input text into any target language (previously: two canned es/vi strings that ignored the input entirely — this closes the "drafts translations" promise/reality gap the upstream docs carried); canned samples remain as the labeled fallback; the qualified-human-translator gate applies to both paths. (3) **Accessibility Review** additionally stores a short model-drafted remediation analysis (`ai_analysis`/`ai_analysis_model`, `serde(default)` so old saved states load) — the five deterministic WCAG rule checks remain the records-bearing floor: AI never adds, removes, or reclassifies a finding; the review's status derives only from the rules; a mid-flight generation failure degrades to deterministic-only and says so rather than blocking the save. **CivicAccess is the suite's first dual-path AI module** (the three existing AI callers hard-stop when the model is absent; this one falls back with an explicit state), implementing the UnifiedSpec non-negotiable "each module degrades gracefully when the LLM is unavailable." New `model.rs::local_generation_available()` readiness helper honoring the `CIVICSUITE_FAKE_MODEL_RESPONSE` test seam. UI: an explicit "AI engine not ready — {status}" banner on the Accessibility tab with a working route to model setup (`data-area="health"`), readiness-aware per-form helper copy, "Multilingual Variant (sample)"/"Create Sample Variant" renamed to match the new behavior, the AI analysis rendered on saved-review rows, and the three AI-capable actions added to `GUIDED_WORK_ACTIONS` (a generation call can hold the global city-work write lock for its full duration, so a human confirm precedes taking that lock — records precedent). Registry: civicaccess `model_needs` flipped from `[]` to the pinned `gemma-4-12b-it-qat-q4_0` `required: true` (contract-enforced id), fallbackState mirror flipped to match, and the **pre-existing civicclerk fallbackState drift fixed** (said `model_required: true` against a registry `required: false` — no parity test guards this field). The four checklist tools (form plan, publishing workflow, ADA Title II, tagged-PDF) stay deterministic on purpose — set-membership/boolean/arithmetic checks a model can only make slower and less reliable. Tests: 9 new cargo tests including the **suite's first not-ready fallback coverage** (no existing AI action had its model-absent path tested), a legacy-state serde round-trip, and a new gated real-runtime CI test (`real_ollama_runtime_with_no_model_serves_the_not_ready_contract_when_enabled`) that boots the payload's real Ollama with zero models and proves the not-ready contract through the real HTTP probe path, not the compile-time stub; 2 new Playwright tests for the not-ready banner (with working model-setup navigation) and the AI-ready framing; module-manager badge asserts for civicaccess (present) and civicclerk (absent); static-smoke phrase pins for the new markers and prompts. Docs: FAQ ADA answer and USER-MANUAL Part 1.6 rewritten for the dual-path behavior (clerk voice + IT voice). 178/178 cargo, 28/28 Playwright, static-smoke + xss PASS, `cargo fmt --check` clean. Upstream civicaccess Python module deliberately stays deterministic v0.4.0 (the standalone Docker product is out of this release's scope; recorded so the "no model calls" self-description there isn't "fixed" by accident).

- **2026-07-01 (later9).** fix(citycore): close GauntletGate round 7's own findings against (later8)'s watchlist fixes on PR #216 -- round 7 held the same zero-tolerance bar and found 4 real Minor/Nit issues in the fixes it was auditing, all now closed: (1) **W7-1:** `.destructive-action` (added in (later8)) was never added to the shared button box-model selector (`desktop/src/styles.css`), so Delete Review rendered with a browser-default border/radius/cursor/padding instead of the app's real button style -- despite a code comment and this changelog claiming "same box model as `.secondary-action`." Fixed by joining the shared selector list; the existing Playwright test only checked className (passes even with an empty CSS rule), so also added a computed-style assertion (border-style/radius/cursor) so this exact regression class can't ship silently again. (2) **W7-3:** the (later8) focus-restoration regression test only exercised nav-click renders -- not representative of the other ~35 render() call sites the fix claims to cover -- added a second test exercising a non-nav render() path (form submission). (3) **W7-4:** the pre-existing `state.appLoadError` render() branch (the "could not open your saved city data" screen) returns before reaching the shared focus-fallback tail, so a load-time failure left focus stuck on `<body>`. Restructured `render()` so both branches share one fallback check at the end instead of the fallback living only in the non-error branch; on the error branch the natural landing target is the Retry button (no `#main-content` exists there). New regression test simulates a `get_app_state` failure and asserts focus lands on Retry. All 3 fixes adversarially verified (revert -> new/strengthened test fails as predicted -> restore -> passes clean). 26/26 Playwright (3 new/strengthened), 169/169 cargo, `cargo fmt --check` clean. Full findings: `gauntletgate-civicaccess-module6-2026-06-30-round7/` (Cowork-local).

- **2026-07-01 (later8).** fix(citycore): close PR #216's full non-blocking watchlist (round 7 -- the project owner asked for zero issues, including nits, not just the 0 Blocker/Critical/Major round 6 already cleared). (1) **W-1, app-wide fix:** every render() call replaced #app's entire subtree with no focus restoration in the general case (only nav-click and the first-run wizard had it) -- traced this to the DOM swap itself dropping focus to <body> at all ~35 call sites app-wide, not just civicaccess. Root-cause fix inside render() itself instead of at each call site: if focus was lost to <body> after a render, restore it to #main-content; verified this doesn't fight maybeAdvanceFirstRunFocus()'s own RAF-deferred field focus (it still wins the next frame, since #main-content is an ancestor of, not inside, the wizard step it checks). Removed the now-redundant explicit focus call at the one site that had one (nav-click). (2) **W-2:** added a reusable `.destructive-action` CSS class (same box model as `.secondary-action`, danger colors) and applied it to Delete Review specifically -- scoped to the actual audit finding, not broadened to other quasi-destructive buttons (Uninstall/Disable/Repair/Stop) that were never flagged. (3) **W-3:** upgraded `state.workActionInFlight` from a single shared scalar to `state.workActionInFlightTags` (a Set) -- confirmed via full read that this scalar is civicaccess-only despite living in the shared dispatcher (no other module reads it), so the upgrade is fully contained. `Set.delete()` is naturally idempotent, so the equality guard the scalar version needed is gone entirely, not just shortened. Left `accessDeleteReviewId` untouched -- it tracks which review the single, app-wide (all ~50 `GUIDED_WORK_ACTIONS`) guided-review confirm panel currently targets, a different, correctly-single-valued concept, not part of this ceiling. (4) **R6-3, verified non-issue, no code change:** `state.workActionResult` is a single "last result" banner shared by every module, same shape as its siblings (`actionResult`, `modelActionResult`, `supervisorActionResult`) -- overwriting it with the most recent result is correct toast-like behavior, not a bug; no scenario names a user-visible wrong result from today's behavior. (5) Completed the version bump `RELEASING.md` requires across all three files: `desktop/package.json` and `desktop/package-lock.json` were still 1.0.1 (only `tauri.conf.json`/`Cargo.toml`/`Cargo.lock` had actually moved to 1.0.2) -- a real gap this round caught. New/updated Playwright coverage for all of the above; cargo/npm/Playwright all green. Full findings: `gauntletgate-civicaccess-module6-2026-06-30-round7/` (Cowork-local).

- **2026-07-01 (later7).** GauntletGate round 6 on PR #216: **0 net-new product defects** — the first of six rounds to break the fix-introduces-a-new-defect chain (44 -> 4 Major -> 1 Blocker+1 Major -> 1 Major -> 1 Major -> **0**). Round 5's `workActionInFlight` guard independently re-verified correct (by direct read and by adversarial revert/restore) four separate ways across the lite lane, the walkthrough lane, and three of five Full-lane roles; the hunt for a third instance of the shared-scalar-cleared-in-a-`finally`-block hazard came back empty via four independent methods, with a structural reason (exactly one `finally` block exists in the whole file, and both scalars it touches are already guarded) rather than an absence-of-evidence argument. Fixed the one net-new item found: a stale timing comment in the round-5 regression test (`workflow-pages.spec.mjs`, said "50ms/400ms," code and a second comment both said 300ms/1500ms). Also fixed a real process gap the round surfaced in the GauntletGate skill itself (not this PR): two Full-lane roles independently performing live adversarial revert/restore cycles raced on the same shared clone, producing a false test failure and a near-miss silent under-restore — added an explicit per-role isolation requirement to the GauntletGate skill (internal skill file, retained project-side) so any role that live-mutates source for its own verification must do so in an isolated copy, not the shared checkout the other roles are reading. **Gate verdict: CLEAR TO ADVANCE** (first time across all 6 rounds). Non-blocking watchlist carried forward unchanged: guided-review/app-wide focus restoration on action re-render, no distinct destructive-button styling, the single-shared-scalar architectural ceiling (already ponytail-commented with its upgrade path). Cargo 169/169, `npm test` PASS, Playwright 21/21. Full findings: `gauntletgate-civicaccess-module6-2026-06-30-round6/` (Cowork-local).
- **2026-07-01 (later6).** fix(citycore): close round-5 GauntletGate findings on PR #216. Round 5 independently live-verified round 4's accessDeleteReviewId guard genuinely holds under adversarial revert/restore and multiple independent from-scratch reproductions -- the round-1→2→3→4 pattern of each fix introducing a new defect in the immediate next round did not repeat a fifth time for that specific fix. But tracing the exact hazard shape round 4 diagnosed (a shared mutable value cleared unconditionally in `handleCityWorkAction`'s `finally` block, clobberable by a different row's action overlapping in flight) surfaced a sibling instance three lines away: `state.workActionInFlight` -- the field driving every civicaccess row's busy/disabled indicator -- had the identical unguarded clear, live-reproduced into a genuine duplicate concurrent backend call (a row's own Delete button falsely re-enabling while its own request was still pending, letting a second click fire a second delete for the same review). Not destructive (idempotent `retain()`-based delete, no duplicate audit entries), but a real correctness gap in a safety/UX invariant across all 9 civicaccess actions. Fixed by mirroring the identical guard already proven for `accessDeleteReviewId` in the same function, plus a new Playwright regression test (an earlier-confirmed, faster-resolving request must not clear a later-confirmed, still-pending request's busy indicator) verified to fail without the guard and pass with it. Documented, not further pursued this round: the shared scalar can still only track one row's busy state at a time (a 3-way-simultaneous overlap would need a Set/Map redesign, not a one-line guard) -- left as a `ponytail:` comment naming the ceiling, since every demonstrated exposure is bounded and non-destructive. Also: the two Desktop\CODE disclaimer drafts' CI-run citations replaced with an explicit fill-at-publish-time placeholder instead of a specific run ID, since a commit can structurally never cite the CI run that validates itself (that run doesn't exist until after the commit is pushed) -- this was recurring every round as "still stale," not a one-time miss. Cargo 169/169, `npm test` PASS, Playwright 21/21 (1 new). Full findings: `gauntletgate-civicaccess-module6-2026-06-30-round5/` (Cowork-local).
- **2026-06-30 (later5).** fix(citycore): close round-4 GauntletGate findings on PR #216. Round 4 independently live-verified round 3's Blocker fix genuinely holds (reverted the one-line `renderGuidedWorkReview()` call, watched the regression test fail with the exact expected assertion, restored it) and found no repeat of the pattern in the render-wiring itself — but found a real Major the round-3 fix introduced: `handleCityWorkAction`'s new `finally` block unconditionally cleared the single shared `accessDeleteReviewId`, so confirming one review's delete while a second review's confirm panel was already open (retargeted, which round 3's own per-row busy design explicitly permits) let the first request's completion silently wipe the second row's target out from under it, making its confirm send an empty reviewId. Not destructive (the Rust handler rejects an empty id), but a real, live-reproduced, silent delete-failure. Fixed by guarding the clear so it only fires if the shared id still matches the request that's completing — one line, same shape as the code it patches — plus a new Playwright regression test that reproduces the exact interleaving with a deliberately-delayed mock backend call, verified to fail without the guard and pass with it. Also: two more regression tests for previously-verified-but-untested edge cases (mid-confirm network error re-enables the row; delete→cancel→delete on the same review re-arms cleanly); `aria-label`s on the per-row buttons now include the review id, since two reviews sharing a title (routinely possible — an empty title is an explicitly supported non-error input) previously collided on an identical label; `playwright.config.mjs`'s dev-server now reuses an already-running instance outside CI instead of hard-failing on "port already in use," a real local-dev friction round 3's new required test step created; a documenting comment on the `channel: "msedge"` / windows-latest coupling; stale test counts and a stale CI run link in both Desktop\CODE drafts (17/17→20/20, one pre-round-3 CI run link) and a "link"→"button" wording fix in USER-MANUAL.md. Not fixed this round (flagged as real but pre-existing/app-wide across all ~50 `GUIDED_WORK_ACTIONS` entries, not introduced by this module's work, and explicitly non-blocking per every reporting role): guided-review panel focus management on open/cancel, and the absence of a distinct visual style for destructive actions. Cargo 169/169, `npm test` PASS, Playwright 20/20 (1 new). Full findings: `gauntletgate-civicaccess-module6-2026-06-30-round4/` (Cowork-local).
- **2026-06-30 (later4).** fix(citycore): close round-3 GauntletGate findings on PR #216 — including a real Blocker this fix pass introduced. Round 2 rewired `civicaccess-delete-review` off `window.confirm()` onto the app's shared `GUIDED_WORK_ACTIONS` confirmation panel, but never added the `${renderGuidedWorkReview()}` call that actually paints that panel — `renderAccessibilityWorkflow()` was the only one of five workflow-area renderers missing it. Result: Delete Review was a 100% silent no-op in both browser-preview and the installed app, with zero user feedback, a regression from the round-1 `window.confirm()` that actually worked. Caught by three independent GauntletGate round-3 sub-agents with live reproduction via a mocked Tauri bridge. Fixed with the missing one-line call, plus a new Playwright regression test (seeds two reviews, clicks Delete on one, asserts the panel renders and shows the right review, clicks Delete on a second review without confirming and asserts the panel retargets rather than stacking or ignoring the click, confirms, and asserts only the targeted review was removed and Cancel leaves data untouched) — verified this test actually fails without the fix and passes with it. Separately: no CI job ran the Playwright browser suite at all (`npm test` is JSON/string contract checks only), which is why this Blocker, and any future JS render/wiring regression, could ship through a fully green, branch-protected CI run undetected — added `npm run test:browser` to the required `build Windows Local MSI` job. Also: an explicit id-keyed multi-review delete assertion in the Rust suite; a Playwright assertion for the 3 newly-required-marked fields and the corrected help-line copy; a Playwright assertion for per-row busy-state isolation; a new pagination Playwright test (20-item cap, show-all/show-fewer toggle); `aria-label`s distinguishing the per-row Export/Delete buttons by review title (same accessibility-module irony round 1 flagged); the guided-review confirm/pending state now also disables a row's own Delete button while its confirmation is open; `accessDeleteReviewId` cleared on Cancel and after the action completes; the delete-review lookup hoisted from an inline IIFE to a top-level `const` matching the ~50 sibling entries' style; the two disclaimer drafts' punctuation brought to a true byte-for-byte canonical match (was reworded-but-not-verbatim); `USER-MANUAL.md` now describes the guided-review delete confirmation and the 20-item pagination. Cargo 169/169, `npm test` PASS, Playwright 19/19 (2 new). Full findings: `gauntletgate-civicaccess-module6-2026-06-30-round3/` (Cowork-local).
- **2026-06-30 (later3).** fix(citycore): close round-2 GauntletGate findings on PR #216 (4 new Major + ~16 Minor/Nit found by an independent re-audit of the round-1 fix; 0 Blocker/Critical both rounds). The round-1 fix patched TEST-10's newline-injection case in one caller; round 2 moved the sanitization into the shared `push_audit()` itself so every module's audit summaries are covered, not just civicaccess's. The new FIFO cap on `audit_events` had frozen the old timestamp+count id scheme into permanent collisions once the cap stabilized — switched to UUIDv4 (matching the precedent already set for review IDs) and added a boundary test. The new `civicaccess-delete-review` confirm dialog used a native `window.confirm()` inconsistent with the app's only other destructive-action pattern (`GUIDED_WORK_ACTIONS`) — rewired through that shared mechanism instead, including row-scoped payload passthrough into the confirm step. A shared help line claimed every required field "doesn't block saving," which was false for 4 of 7 forms (Plain-Language, Multilingual x2, ADA Title II) — softened the copy to be accurate for both behaviors and added the missing `aria-required`/`*` markers to the 3 fields that actually were hard-required but unmarked. Also: pagination on the saved-review list (20 at a time, "show all" toggle) since only the audit-events mirror had a cap, not the reviews themselves; a regression test proving a civicaccess-only profile can reach `search-city-knowledge` (TEST-3's prescribed test, never added even though its refutation held); per-row busy-state now keyed by action+reviewId so unrelated rows don't disable together; `:disabled` button styling; a Rust/JS disclaimer-string mismatch (the Rust constant was unused but worded differently — now byte-identical); a redundant "open the desktop app" phrase appearing twice in one message; the two remaining un-fixed disclaimer drafts (announcement + release notes) brought to the verbatim canonical sentence; release notes' unsupported "planned v1.0.3" claim softened to "not yet scheduled." Cargo 169/169, `npm test` PASS, Playwright 17/17. Full findings: `gauntletgate-civicaccess-module6-2026-06-30-round2/` (Cowork-local).
- **2026-06-30 (later2).** fix(citycore): close the GauntletGate full-lane punch list on PR #216 (5 Critical + 18 verified Major/Minor/Nit; one Test-role finding each on module guards and the search-city-knowledge allow-list was investigated and refuted against the actual source). Highlights: `aria-current="page"` on the active primary-nav item; per-field input-length caps on all 8 civicaccess handlers mirroring upstream Pydantic limits (title 500 / body+text 5000 / language 80 / list items 100-200, with a max-item-count cap) closing an unbounded-paste DoS surface; `main` branch protection now requires the MSI/integration/lifecycle checks in addition to `verify`; `records-export` renamed to `civicaccess-records-export` (collision-safe prefix) with a matching module guard, dispatcher arm, and UI button; review IDs switched from a predictable timestamp+counter scheme to UUIDv4; new `civicaccess-delete-review` action + UI button + module guard (with a confirm prompt) so saved reviews can be removed; FIFO cap on the per-module `audit_events` mirror at 5,000 entries; `next_steps` + `disclaimer` fields added to action results and populated for every civicaccess handler; form labels associated via `for`/`id` with `aria-required` + a top-of-section help line on every required field; a primary-action button on "Run Review & Save"; a single canonical advisory-disclaimer sentence used everywhere (in-UI banner + per-row + empty state + `USER-MANUAL.md` + `FAQ.md`); status pills mapped to clerk-readable labels; a loading/`aria-busy` state on in-flight workflow actions; clearer browser-preview "desktop app required" guidance across all city-work actions; new USER-MANUAL.md Part 1.6 walkthrough for the Accessibility tab; new FAQ.md entry on ADA Title II compliance; six additional Playwright assertions (one per remaining form-submit button) plus a malformed-payload-fallback assertion; five additional cargo assertions for previously-untested branches (tagged-PDF empty-payload, publishing-workflow and ADA happy paths, the `vi` multilingual sample, the 80-word long-copy finding) and full coverage of the jargon map plus its title-cased forms; a newline-injection adversarial test on the audit log; CI lifecycle job now also runs the civicaccess cargo suite and checks for real per-user-data leakage after uninstall. Cargo 167/167, `npm test` PASS, Playwright 17/17. Companion fixes pushed to PR #217 (reverse-direction `fallbackState` drift-guard check) and PR #215 (historical-snapshot callout on the 2026-06-29 deep-read audit). Full findings + fix paths: `gauntletgate-civicaccess-module6-2026-06-30/REPORT.md` (Cowork-local).

- **2026-06-30.** feat(citycore): CivicAccess v1.0.2 desktop UI integration — the clerk-facing Accessibility workflow tab now ships in the desktop shell, closing the v1.0.1 documentation/UI gap surfaced by the [2026-06-29 deep-read audit](docs/audits/civicaccess-citycore-deep-read-2026-06-29/FINAL-REPORT.md). Mirrors the CivicNotice integration shape: native Rust port of `access_review.py` + `plain_language.py` + `multilingual.py` + `workflows.py` into `desktop/src-tauri/src/workflows.rs` against a new `state.access` field on `CityWorkState`; `renderAccessibilityWorkflow()` panel with seven `data-work-action` forms (WCAG review, plain-language rewrite, multilingual variant, accessible form plan, publishing-workflow checklist, ADA Title II plan, tagged-PDF expectations) plus a saved-review list with a per-row records-ready export button; `Accessibility` primary-nav entry; `civicaccess` added to `MODULE_AREA_BY_ID` + `CITY_CORE_PRODUCT_MODULE_IDS` + `fallbackState.modules` + `installed_module_ids` + `search-city-knowledge` (CivicAccess reviews now surface in cross-module Search alongside meetings/records/code/notices). CivicCore audit chain extended with a per-module `audit_events` vec mirroring civicaccess's standalone Postgres `audit_events` table. Version bump `1.0.1 -> 1.0.2` in `Cargo.toml` + `tauri.conf.json`. Tests: cargo end-to-end (`civicaccess_actions_save_review_export_and_emit_deterministic_findings` + new `civicaccess_actions_reject_invalid_inputs` covering 11 trust-boundary error paths, 167/167 cargo pass) + Playwright spec (`civicaccess accessibility tab renders the seven workflow forms and refuses persistence from preview`, 17/17 pass). Phase B token + schema + role wiring is consumed as-is; no civicaccess re-cut needed (still v0.4.0, source_commit 7b24516). Companion fixes in the same PR (#216) sweep pre-existing module-count parity bugs in the fallbackState city-core profile description, `renderModuleSelection()` profile-choice strings, the Search-page lead text, and the civicaccess module-card metadata (route/service/task counts aligned with canonical `installer/modules.json`).

- **2026-06-30 (later).** fix(ui): `fallbackState` task_counts for `civicrecords-ai` (6→4) and `civicclerk` (6→5) now match canonical `installer/modules.json`. Pre-existing parity bugs since CivicNotice landed (2026-06-13); fallbackState is only consumed in browser preview so drift was silent in the shipped desktop. Plus a new static-smoke meta-test in `desktop/tests/static-smoke.mjs` that iterates every `fallbackState.modules` entry, joins it with `installer/modules.json` by module id, and asserts `route_count` / `service_count` / `task_count` match — locks this class of drift permanently. Self-contained 2-file fix on `fix/fallbackstate-task-counts-parity` (PR #217); no overlap with PR #216.

- **2026-06-29 (later).** docs: clarify CivicAccess UI integration status across `USER-MANUAL.md`, `README.md`, `ANNOUNCEMENT.md`, `FAQ.md`, and `STATUS.md`. The 2026-06-29 city-core promotion (entry below) flipped registry truth 5→6, and the next MSI build bundles the CivicAccess module code, database schema, and write-token secret — but the on-screen Accessibility workflow tab in the desktop UI lands in **v1.0.2**. Phase B ([PR #213](https://github.com/CivicSuite/civicsuite/pull/213)) wired the runtime; Phase C ([PR #214](https://github.com/CivicSuite/civicsuite/pull/214)) flipped the registry; neither added UI panels in `desktop/src/main.js` or `city_work_action` handler arms in `desktop/src-tauri/src/workflows.rs`. The v1.0.2 PR will mirror the [CivicNotice precedent #193](https://github.com/CivicSuite/civicsuite/pull/193). Full evidence: `docs/audits/civicaccess-citycore-deep-read-2026-06-29/FINAL-REPORT.md` (35-agent deep-read of the entire CivicSuite org).

- **2026-06-30 (later).** infra(hooks): `scripts/hooks/pre-push.ps1` now runs `cargo fmt --check` against `desktop/src-tauri` when the push range includes `desktop/src-tauri/src/*.rs` changes. Catches Rust-formatting drift locally (~5s) instead of in the MSI CI workflow step 4 'Check Rust formatting' (~1hr wasted per occurrence). Surfaced by the 2026-06-30 PR #216 MSI build failure (run 28437357522) where my civicaccess additions hadn't been fmt'd locally first. The check is scoped to the actual push range so it only runs when relevant. Skipped silently if cargo isn't on PATH.

- **2026-06-29.** release/installer: promote **CivicAccess v0.4.0** (`source_commit 7b24516fd89584d84c12394b9385eddd1e8c6897`) to the **sixth city-core module** after a passing depth re-probe reversed the 2026-05-23 NEEDS-WORK demotion. The city-core profile is now `civiccore, civicrecords-ai, civicclerk, civiccode, civicnotice, civicaccess`; the registry, module contract, every hard-coded 5-module gate (`verify-suite-state.py`, `verify-installer-plan.py`, the desktop cargo tests, the contract test), the compatibility matrix, the UnifiedSpec, and the public truth surfaces were flipped to six in one lockstep changeset, and `verify` + the desktop cargo tests pass. This is a truth/registry milestone, **not** the program Definition of Done (clean-VM acceptance is Phase D). The current published v1.0.1 MSI bundles the first five city-core modules; the six-module MSI is the next build.

- **2026-06-28.** release: open CivicSuite Windows Local **1.0.1** as a **GA candidate / public beta**. The `civicsuite-windows-local-v1.0.1` release (Latest; MSI SHA-256 `5a1e5e2e4d2f3d7f77c52f108c4445c85db10ff3edc2c151d6bbae1cd97ce3ea`, 1,645,479,777 bytes) was validated end-to-end on a clean Windows Sandbox (QA-B1 PASS: install -> first-run -> 6.97 GB model download/verify/load -> real AI completion -> clerk records round-trip -> backup/restore -> uninstall) and closed two adversarial readiness audits at zero open Blocker or Critical findings (remaining items are documented, non-blocking follow-ups). The build is opened for public hands-on use now; the one remaining gate to General Availability is Authenticode code-signing, in progress via the SignPath Foundation (see `CODE_SIGNING_POLICY.md`), so the MSI ships unsigned for the beta. This is **not** a public-use, city-ready, procurement-ready, production-ready, macOS lifecycle, or full-suite promotion claim; CivicAccess remains out of city-core and Tier 2 modules remain queued. Public announcement copy in `ANNOUNCEMENT.md`.

---

## [civicsuite-windows-local-v1.0.1] - 2026-06-26

Security-critical patch to the Windows-local beta desktop build, published as
`civicsuite-windows-local-v1.0.1` (built from `bae1111a`) and promoted to Latest.
It supersedes `civicsuite-windows-local-v1.0.0` and retires the
`windows-local-msi-firstrun-fix-rc1` candidate, whose MSI predated these fixes.

This remains a **beta** build. It does not claim public-use readiness, city-ready
status, procurement/production readiness, macOS lifecycle certification, or
full-suite release.

### Hardening (post-criticals beta pass)

- Build/installer version raised `0.1.0` → `1.0.1` to match the release label
  (ARP / ProductVersion now read `1.0.1`; the same-version-upgrade WiX contract is
  unchanged).
- Defense-in-depth: remaining backend-origin strings are `escapeHtml`-escaped at
  the app-chrome / setup / model / module render sinks (city-work surfaces were
  already escaped by C1).
- Local sign-in passcode throttle/lockout (5 failures → 60s cooldown, reset on
  success; constant-time compare preserved).
- External Windows system binaries are spawned by absolute `%SystemRoot%` paths
  instead of bare names (PATH-hijack hardening) across the renderer/model paths
  (`model.rs`, `local_shell.rs`) and the service supervisor (`supervisor.rs`:
  `powershell.exe`, `compact.exe`, `tasklist.exe`, `taskkill.exe`).
- The MSI unsigned-beta install notice lists all five city-core modules
  (adds CivicNotice), matching the documented set.
- First-run model step now states the ~6.97 GB download size, a rough time
  estimate, and that it resumes if interrupted; a non-fatal low-RAM preflight
  warning mirrors the existing low-disk preflight.

### Security

- **C1** — stored/reflected XSS hardening across the desktop (Tauri/WebView2)
  renderer surfaces, with an added browser XSS backstop test
  (`desktop/tests/browser/xss-backstop.spec.mjs`, `desktop/tests/xss-and-state.mjs`).
- **C2** — atomic state writes, eliminating torn/partial state files on a crash
  or power loss mid-save.
- **C3** — single-instance enforcement, preventing concurrent instances from
  corrupting shared local state.
- **ENG01/ENG02/ENG03** — supporting engineering hardening landed alongside the
  criticals.

### Added (CI)

- **T-C1** — the desktop↔CivicCore real-runtime integration test now actually
  runs (prepares the portable PostgreSQL/worker payload and exercises the wired
  contract) instead of being a silent no-op when its env gate was unset.
- **T-C2** — the Windows MSI is now installed, exercised through
  first-run/backup-restore lifecycle logic, and uninstalled in CI. The main
  binary is discovered by scan (`civicsuite-desktop.exe`, the Cargo binary name)
  rather than a hardcoded `CivicSuite.exe`, and the install dir is resolved from
  the ARP `DisplayIcon` when `InstallLocation` is empty.

### Validated (QA-B1, clean machine)

- The **shipped `CivicSuite_1.0.1_x64_en-US.msi`** was validated **end-to-end** on a
  **fresh Windows Sandbox with no prior CivicSuite install** (20 GB RAM, disposable
  Windows): install (`msiexec /i` exit 0) → app launches and the WebView2 window
  renders → single-instance holds on a 2nd launch → full first-run wizard (city
  profile, first admin, modules) → **6.97 GB Gemma model download + checksum +
  Ollama load → a real local AI completion** → a clerk **records-intake workflow**
  (submit + look up a public records request, round-trip through PostgreSQL) →
  **backup + restore** → uninstall. Verdict: **PASS** (all checks). An earlier
  install/verify/uninstall-only pass also covered the 0.1.0-internal pre-bump build.

### Artifacts

- `CivicSuite_1.0.1_x64_en-US.msi` — SHA-256
  `5a1e5e2e4d2f3d7f77c52f108c4445c85db10ff3edc2c151d6bbae1cd97ce3ea`
  (1,645,479,777 bytes); ARP/installer version reads **1.0.1**. This is the exact
  artifact clean-machine-validated end-to-end above and attached to the
  `civicsuite-windows-local-v1.0.1` release. Unsigned beta MSI; SmartScreen
  guidance is "More info → Run anyway"; no Docker or WSL prerequisite.
- Retired `windows-local-msi-firstrun-fix-rc1` MSI for the record:
  SHA-256 `2f5038298a1ff36a901b885010243ab36c30e068ba36ac225907b1f98d4955cb`,
  built `2026-06-25T23:50:50Z` (pre-fix); the unpatched installer was removed
  from that release.

---

## Historical entries (preserved; framing reset by 2026-05-09 recovery notice above)

The following entries are kept as historical record. Their inline "shipping," "compatibility publication," "v1.0.0," and similar promotion language refers to release labels that are currently **frozen pending recovery**. Do not read these as ship signals.

### Added (pre-recovery)

- **Deployment profile wheel-contract fix** (2026-05-06): the post-foundation
  Docker demo now pins CivicCode `v0.1.18` to the published `civiccore v0.22.0`
  wheel required by that release artifact, and the deployment-profile verifier
  now inspects module wheel metadata so CI catches compose/package dependency
  mismatches before runtime boot.
- **CivicSuite truth-source refresh after audit-full** (2026-05-06): compatibility
  matrix, suite-state verifier, deployment-profile verifier, demo compose pins,
  README, README.txt, user manual, roadmap, and landing page now record
  CivicCore `v1.0.0`, CivicClerk `v1.0.0`, CivicCode `v0.1.18`, and the
  current local CivicNotice `v0.1.2` split instead of stale pre-1.0 values.

  *(Recovery note: these label changes are doc reflections of release tags now under recovery freeze.)*

- **CO-7 CivicCore freeze lockstep** (2026-05-05): compatibility matrix,
  suite-state verifier, and unified spec now record CivicCore `v0.22.1` as the
  attested baseline and document the placeholder-namespace audit before the
  freeze-line tag.

  *(This entry is real and is the most defensible "shipping" claim in the cluster — v0.22.1 is the attested baseline. v1.0 carries that attestation forward but has not re-earned it under the recovery gates.)*

- **CivicCode v0.1.17 compatibility publication** (2026-05-04) — *doc-update entry; CivicCode tag frozen pending recovery.*
- **CivicCode v0.1.16 compatibility publication** (2026-05-04) — *doc-update entry; CivicCode tag frozen pending recovery.*
- **CivicCode v0.1.15 compatibility publication** (2026-05-04) — *doc-update entry; CivicCode tag frozen pending recovery.*
- **CivicCode v0.1.14 compatibility publication** (2026-05-04) — *doc-update entry; CivicCode tag frozen pending recovery.*
- **CivicCode v0.1.13 compatibility publication** (2026-05-04) — *doc-update entry; CivicCode tag frozen pending recovery.*
- **CivicCode v0.1.12 compatibility publication** (2026-05-04) — *doc-update entry; CivicCode tag frozen pending recovery.*
- **CivicCode v0.1.7 compatibility publication** (2026-05-03) — *doc-update entry; CivicCode tag frozen pending recovery.*
- **CivicCode v0.1.6 compatibility publication** (2026-05-03) — *doc-update entry; CivicCode tag frozen pending recovery.*
- **CivicCore v0.21.0 / CivicRecords AI v1.4.8 / CivicClerk v0.1.19 compatibility publication** (2026-05-03) — *doc-update entry; tags frozen.*
- **CivicCore v0.20.0 / CivicRecords AI v1.4.7 / CivicClerk v0.1.18 compatibility publication** (2026-05-02) — *doc-update entry; tags frozen.*
- **CivicCode v0.1.5 compatibility publication** (2026-05-02) — *doc-update entry; tag frozen.*
- **CivicCode v0.1.4 compatibility publication** (2026-05-02) — *doc-update entry; tag frozen.*
- **CivicCode v0.1.2 compatibility publication** (2026-05-02) — *doc-update entry; tag frozen.*
- **CivicCore v0.19.0 / CivicRecords AI v1.4.6 / CivicClerk v0.1.17 compatibility publication** (2026-05-02) — *doc-update entry; tags frozen.*
- **CivicCore v0.18.1 / CivicRecords AI v1.4.5 / CivicClerk v0.1.16 compatibility publication** (2026-05-02) — *doc-update entry; tags frozen.*
- **Roadmap precision after CivicClerk OIDC/live-sync completion** (2026-05-02) — *the roadmap-precision entry is real; the underlying claim that "OIDC browser-session support and vendor-network live sync" are shipped foundations is mock-validated, not production-validated. See STATUS.md and DOC-024 in audit-civicsuite-2026-05-09.*
- **CivicRecords AI v1.4.4 compatibility publication** (2026-05-01) — *doc-update entry. The phrase "the published shipping records product" used in the original entry is overstated; civicrecords-ai's own README labels v1.4.10 as "do-not-promote."*
- **CivicClerk v0.1.13 compatibility publication** (2026-05-01) — *doc-update entry; tag frozen.*
- **CivicRegWatch and CivicAPI planning specs** (2026-04-30) — *real entry. Specs added; no runtime work yet.*
- **Compatibility matrix catch-up for active CivicCore rollout lines** (2026-04-29) — *real entry.*
- **Continuity gate and canonical roadmap reset** (2026-04-29) — *real entry. SUCCESSION.md added; this is the original honesty-framing intervention.*
- **Phase 0 continuity closeout** (2026-04-29) — *real entry. Second org owner added.*

- **CivicRecords AI v1.4.1 compatibility update** (2026-04-28): compatibility matrix and downstream docs updated to reflect the records patch release. *Real entry, but the "shipping" framing predates the recovery freeze.*
- The **2026-04-27 to 2026-04-28 cluster** of v0.1.0 → v0.1.1 module-foundation publications represents real foundation-tier release work across the catalog. These tags exist and are not under recovery freeze (v0.1.x is the foundation tier, which has not been claimed as product-ready). They are best read as: "this module repo now exists with the minimum scaffolding."
- **Post-foundation sequence correction** (2026-04-27) — *real entry.*
- **Post-foundation hardening plan** (2026-04-27) — *real entry.*
- **Suite-state verifier** (2026-04-27): `scripts/verify-suite-state.py` added. *Real entry.*
- **Local demo deployment profile** (2026-04-27) — *real entry.*
- **Shared shell boundary** (2026-04-27): ADR-0004 + shared shell UX inventory added. *Real entry.*
- **Connector import/export boundary** (2026-04-28): ADR-0005 + suite connector template added. *Real entry.*
- **CivicCore v0.3.0 extraction proposal** (2026-04-28): ADR-0006 + bounded proposal added. *Real entry.*
- **CivicClerk production-depth workflow plan** (2026-04-28): ADR-0007 + first integrated production-depth sprint plan added. *Real entry.*

- Added `docs/CivicSuiteUnifiedSpec.md` as the canonical suite specification.
- **CivicClerk scaffold registration** (2026-04-26): `CivicSuite/civicclerk` created.
- **Phase 2 documentation closeout** (2026-04-25): full set of community files, GitHub issue templates, PR template, GitHub Discussions seed posts.
- Suite orientation manual `USER-MANUAL.md` (three parts).
- Plain-text `README.txt` companion.
- `scripts/verify-docs.sh` — required-artifact and stale-current-facing-string check.

### Changed (pre-recovery)

- Registered `CivicSuite/civiccode` scaffold and Milestone 0 completion across umbrella docs while preserving the no-runtime-shipped boundary.
- Updated roadmap and unified spec current-state sections after CivicClerk v0.1.0 and `/staff` UI foundation shipped.
- **CivicClerk staff workflow UI foundation reflected in suite docs** (2026-04-27): umbrella docs now mention the `/staff` browser UI foundation while preserving the full-workflow-UI planned boundary.
- **CivicCode v0.1.0 compatibility update** (2026-04-27).
- **Compatibility matrix updated for CivicClerk v0.1.0** (2026-04-27).
- **Compatibility matrix updated to current truth.**
- **Landing page (`docs/index.html`) refreshed.**
- README rewritten to lead with current suite status (this was the original honesty-framing pass).

## [Phase 1] - 2026-04-24

### Added
- Initial scaffold of the civicsuite umbrella repository (Phase 0).
- Charter, consistency reference, and four canonical specs (catalog, CivicCore extraction, CivicClerk, CivicZone) imported from the workspace draft.
- Empty `docs/` skeleton with stub index files for catalog, principles, architecture, roadmap, governance, and compatibility.
- ADR-0001: extract CivicCore as a non-breaking refactor before any second module starts.
- LICENSE (CC BY 4.0) for documentation; LICENSE-CODE (Apache License 2.0) for example code snippets.
- CONTRIBUTING.md with the bug-routing decision tree from CivicCore Extraction Spec section 18.

### Changed
- License for code switched from MIT to Apache License 2.0.
- Three doc-drift fixes flagged by audit review of Day-3 inventory.
- Spec 02 sections 8 and 9 updated to match actual civicrecords-ai paths.
- CONSISTENCY.md drift-watch item 6 added.
- ADR-0002 and ADR-0003 added; ADR-0003 substantially rewritten.
- Compatibility matrix updated after Phase 1 ship: `civiccore` recorded at `0.1.0`.
